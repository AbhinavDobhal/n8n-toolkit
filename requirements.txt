# Audio Production Pipeline - Python Dependencies
# Install with: pip3 install -r requirements.txt

# Core audio processing
pydub>=0.25.1

# HTTP requests for TTS API
requests>=2.28.0

# Optional: FFmpeg Python wrapper (fallback if direct FFmpeg not available)
# Note: FFmpeg binary must be installed separately (see setup.sh)
# python-ffmpeg>=1.0.0

# Optional: Advanced audio processing
# soundfile>=0.12.1
# librosa>=0.10.0
# scipy>=1.10.0

# Optional: Async support
# aiohttp>=3.8.0
# asyncio>=3.4.3

# Development/Testing (uncomment if needed)
# pytest>=7.0.0
# black>=23.0.0
# pylint>=2.16.0
